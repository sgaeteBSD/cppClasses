#include "Media.h"

class Music : public Media {
private:
  char* artist;
  char* publisher;
  int duration;
public:
  Movie(char* title, int year, char* director, int duration, int rating);
  virtual ~Movie();

  virtual void print();

  char* getDirector();
  int getDuration();
  int getRating();
}
